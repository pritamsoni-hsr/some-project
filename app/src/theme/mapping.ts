import { ApplicationProviderProps } from '@ui-kitten/components';

const mapping: Partial<ApplicationProviderProps> = {
  customMapping: {
    components: {},
  },
};

// to make ts happy
export default { ...mapping };
